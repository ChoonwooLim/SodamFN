# SodamApp Backend Dependencies
# FastAPI & Server
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.20
python-dotenv==1.1.1

# Database
sqlmodel==0.0.31
SQLAlchemy==2.0.42
psycopg2-binary==2.9.11
alembic==1.16.4

# Data Processing
msoffcrypto-tool==5.4.2
pandas==2.3.1
openpyxl==3.1.5
xlrd==2.0.1
lxml==5.3.1

# PDF Parsing
pypdf==5.6.0
pdfplumber==0.11.6

# HTTP Requests
requests==2.32.4
httpx==0.28.1

# curl_cffi — Chrome TLS fingerprint 위조 (Akamai/Cloudflare 우회용)
# 쿠팡이츠 매출 API 직접 호출 (httpx 의 표준 TLS 핑거프린트는 봇 탐지에 노출됨)
curl_cffi>=0.7.0

# Playwright — Akamai sensor 통과용 헤드리스 Chromium
# 쿠팡이츠 사장님 포털 자동 로그인 (sensor_data POST 가 브라우저 환경 검증)
playwright>=1.49.0
playwright-stealth>=1.0.6

# Security
cryptography==45.0.6
PyJWT==2.10.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utilities
python-dateutil==2.9.0.post0
pytz==2025.2
Pillow==11.3.0
boto3>=1.35.0

# CORS
starlette==0.35.1
gunicorn

python-dotenv

# Popbill SDK (팩스·문자·세금계산서·계좌조회 등 팝빌 연동)
popbill>=1.64.0

# CODEF SDK (마이데이터 통합 — 카드 매출/계좌/4대보험 자동수집)
easycodefpy>=0.5.0

# HTML → PDF 렌더링 (증명서 팩스 전송용)
weasyprint>=60.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0

# Year-end PDF generation
weasyprint==63.1
Jinja2==3.1.4

