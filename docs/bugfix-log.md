# 버그 수정 로그

| 날짜 | 버그 | 원인 | 수정 내용 | 관련 파일 |
|------|------|------|-----------|-----------|
| 2026-04-19 | 서류 업로드 실패 | 로컬 디스크 폴백이 서버 환경에서 불일치 | 로컬 폴백 제거, 미디어 서버 전용 저장 | backend 서류 업로드 로직 |
| 2026-04-19 | 서류 업로드 multipart 오류 | Content-Type 헤더에 boundary 누락 | multipart boundary 자동 설정 + 에러 핸들링 개선 | backend 서류 업로드 로직 |
| 2026-04-19 | 한글 파일명 서류 업로드 500 오류 | 미디어 서버에서 한글 파일명 처리 실패 | 파일명 인코딩 처리 수정 | backend 서류 업로드 로직 |
| 2026-04-19 | 보건증 라벨 오류 | 라벨 텍스트가 구 명칭 사용 | 보건증 → 건강진단서로 변경 | 프론트엔드 UI |
| 2026-04-19 | 급여 실수령액 이중 계산 | PayrollStatement에서 total_pay + deductions로 중복 합산 | total_pay만 사용하도록 수정 | PayrollStatement.jsx |
| 2026-04-19 | 세금대납 급여 계산 오류 | 세금대납 시 실수령액 표기와 이체액 혼동 | 총 보상액/실수령액/계좌이체 3단계 분리 | PayrollTab.jsx, PayrollStatement.jsx, payroll.py, banking_service.py |
| 2026-04-19 | HR 대시보드 직원 데이터 0명 표시 | Axios 응답이 {status, data:[...]} 인데 .data로 한 번만 추출하여 객체를 배열로 취급 | Array.isArray 체크 + rawData?.data 추출 추가 | HRDashboard.jsx |
| 2026-04-19 | 퇴직금 탭 재직 직원 급여 내역 미표시 | contract_end_date(미래)를 calc_end_date로 사용하여 3개월 윈도우가 미래로 밀림 | min(contract_end_date, today)로 제한 | retirement.py |
| 2026-04-19 | 매장관리 매장 목록 사라짐 | SQLModel create_all이 기존 테이블에 컬럼 추가 불가 → employee_scale 누락 → Business 쿼리 전체 500 에러 | ALTER TABLE business ADD COLUMN employee_scale | models.py, PostgreSQL |
| 2026-04-19 | 직원앱 프로필 등급이 DB 변경 후 갱신 안 됨 | JWT payload의 stale grade만 읽어 표시 | /auth/me 재조회로 최신 grade 병합 | staff-app/Profile.jsx |
| 2026-04-19 | 연차 승인 시 잔액 차감이 원자적이지 않고 타 사업장 요청까지 승인 가능 | 승인/차감이 분리된 커밋 + 테넌트 필터 누락 | 단일 트랜잭션 + X-View-As-Business 테넌트 검증 + 잔액 부족 즉시 반려 | leave.py |
| 2026-04-19 | 사업장 규모 설정 API 404 (PUT /api/api/auth/business-settings) | useBusinessConfig가 `/api/...` 경로 사용, 그러나 axios baseURL 이미 `/api` 포함 → 이중 prefix | 경로를 `/auth/...`로 정정 + HR 토글 [현재] 배지 UI | useBusinessConfig.jsx, HRDashboard.jsx |
| 2026-04-19 | 사업장 규모 변경 후 화면에 반영 안 됨 (피드백 없음) | 성공/실패 메시지 미표시 + Context 재조회 누락 | scaleMessage 배너 + refreshBusinessConfig() 호출 | Settings.jsx |
| 2026-04-19 | SuperAdmin의 사업장 규모 변경 시 400 Bad Request | admin.business_id=None 이라 서버가 대상 사업장을 특정 못함 | PUT /auth/business-settings에서 X-View-As-Business 헤더로 bid 결정 | auth.py |
| 2026-04-19 | 5인 미만 사업장 직원이 무급휴가·병가·경조사도 신청 불가 | _resolve_self_staff가 under5 전체를 블록 | 연차(ANNUAL_TYPES)만 차단하고 무급/병가/경조사 허용, 튜플 반환으로 변경 | leave.py |
| 2026-04-24 | 팩스 전송 시 생성된 PDF가 빈 페이지로 나감 | 완전 HTML 문서(`<html>/<body>/<style>`)를 `<div>.innerHTML`에 통째로 넣어 브라우저가 중첩된 html/body 태그를 무시 | DOMParser로 파싱해 `<style>` 태그와 `body.innerHTML`을 분리 추출 후 컨테이너에 각각 별도 주입, `document.fonts.ready` + 250ms 대기로 폰트/이미지 로딩 확보 | FaxTransmission.jsx |
| 2026-04-24 | 소담김밥 Business 레코드가 비어있어 증명서에 대표자 `Admin` / 사업자번호 `-` 로 출력 | 가입 시 디폴트 값만 있고 실제 회사정보 편집 UI가 없었음 | 회사정보 관리 탭 신설 + Business 1 레코드에 실값(639-12-01514 / 홍지연 / 02-452-6570 / 주소) 주입 | CompanyInfoSettings.jsx, auth.py |
| 2026-04-24 | Business.phone 이 `02-452-6510` 로 잘못 저장됨 | 손글씨 이미지 OCR 오판독 (7→1) | DB + `.env` POPBILL_SENDER_NUMBER 모두 `02-452-6570` / `0224526570` 으로 정정 | DB update, backend/.env |
| 2026-04-24 | 팝빌 팩스 전송 성공 코드가 오지만 실제 수신되는 페이지는 빈 용지 | html2pdf.js(html2canvas)가 복잡한 한글 CSS + SVG 필터 + mm 단위 레이아웃을 캡처하지 못해 24byte content stream / 0 XObject 짜리 빈 PDF 생성. DOMParser 분리 방식도 실패 | Dockerfile에 libpango/libharfbuzz/libfribidi + fonts-nanum/noto-cjk 추가 후 WeasyPrint로 서버사이드 HTML→PDF 전환. `GET /hr/certificate/pdf/{cert_type}/{staff_id}` 엔드포인트 신설, 프론트에서 html2pdf.js 완전 제거 | Dockerfile, requirements.txt, certificate.py, FaxTransmission.jsx |
| 2026-04-24 | WeasyPrint PDF에 업로드한 직인 이미지가 `직인` 텍스트로만 표시 | settings_json.seal_image_url이 상대 URL(`/api/media/...`)이라 WeasyPrint가 컨테이너 내부에서 resolve 실패 → `<img>` alt 텍스트로 fallback | `_fetch_image_as_data_uri()` 헬퍼로 FRONTEND_URL 기준 다운받아 base64 data URI embed. 실패 시 SVG fallback | certificate.py |
| 2026-04-24 | 증명서가 2페이지로 분리되어 1페이지에 대표자·직인이 없음 | cert-issuer 블록이 page-break-inside:avoid라 1페이지에 공간 부족 시 2페이지로 통째로 밀림 | @page margin 20→12mm, cert-wrap padding/min-height 제거, 전반 스페이싱 축소로 A4 1페이지 수렴 | certificate.py |
| 2026-04-24 | 환경설정 → 회사정보 관리 탭의 직인 이미지 미리보기가 업로드 후에도 비어 보임 | `get_business_info` 응답에 `seal_image_url` 필드 누락. 실제 DB에는 저장됨(증명서 PDF에는 반영 확인) | auth.py::get_business_info 응답에 settings.seal_image_url 포함 | auth.py |
| 2026-04-25 | bank-sync API 호출 시 500 에러 (팝빌 SDK 메서드명 불일치) | EasyFinBankService 실제 메서드명/시그니처 확인 없이 추측한 이름·인자 순서로 호출 | popbill SDK 소스 참조해 listBankAccount/getBankAccountInfo/requestJob/getJobState/search/summary 시그니처 재구성 | bank_sync_service.py |
| 2026-04-25 | bank-sync 진단 모달의 에러 상세가 작은 글씨/접힘 상태로 출력 | nested error 객체 details가 디폴트 collapsed + 폰트 작음 | 에러 발생 시 자동 펼침, 폰트 12→14, 코드 블록 가독성 개선 | BankSync.jsx |
| 2026-04-25 | bank-sync 진단 결과가 내부 ok=False/skipped여도 UI에 "성공"으로 표시 | 최상위 ok 플래그만 보고 success 분기 → 내부 step 단위 실패 누락 | step별 ok=False or status='skipped' 검사 추가, 하나라도 실패면 전체 실패 | BankSync.jsx |
| 2026-04-25 | 어드민 /yearend 페이지가 "대상 직원이 없습니다" 출력 (실제 19명 + Payroll 54건 존재) | 16 엔드포인트 모두 `biz_id = get_bid_from_token(request)` 직접 호출. get_bid_from_token 은 FastAPI Header dependency 함수 → Request 객체를 authorization 인자로 받으면 내부 예외로 항상 None 반환 → `Staff.business_id == None` 필터링 → 0 row | 16개 엔드포인트 시그니처에 `biz_id: Optional[int] = Depends(get_bid_from_token)` 주입, body 의 직접 호출 라인 제거. 다른 라우터(cashbill 등)와 동일 패턴 | routers/yearend.py |
| 2026-04-27 | 팝빌 EasyFinBank Orbitron 재배포해도 TEST 모드 진입 안 됨 (POPBILL_BANK_IS_TEST 미설정 시 LIVE 강제 → -99010016 차단) | Orbitron 대시보드 env 자동 주입 미지원 + bank_sync_service.py 의 is_test 결정 로직이 POPBILL_BANK_IS_TEST 미설정 시 무조건 false(LIVE) 강제 → POPBILL_IS_TEST=true 가 이미 있어도 무시 | bank_sync_service.py __init__ 에서 POPBILL_BANK_IS_TEST 빈값일 때 POPBILL_IS_TEST 값을 fallback. /diagnose 응답에 is_test_source 필드 추가 (사용 변수 즉시 식별 가능) | services/bank_sync_service.py, routers/bank_sync.py |
| 2026-04-27 | SuperAdmin 로그인 직후 페이지가 잠깐 떴다가 즉시 /login 으로 튕김 (일반 admin 은 정상) | 4/25 영업관리 V1 빨간 배지 도입 시 Sidebar useEffect 가 모든 페이지에서 GET /api/sales-guide/stats 호출. SuperAdmin 은 business_id 없고 X-View-As-Business 헤더도 없어 백엔드 401 → api.js 인터셉터가 토큰 비우고 강제 로그아웃 | useEffect 진입 시 role==='superadmin' && !viewAsBid 면 호출 스킵 + alerts=0. view-as 매장 선택 후엔 정상 호출. dependency 에 viewAsBid 추가 | components/Sidebar.jsx |
| 2026-04-27 | 예금주조회 단건 호출 시 AttributeError 'AccountCheckService' object has no attribute 'CheckAccountInfo' — 코드 자체가 깨진 상태 | popbill SDK 1.64.1 은 lowerCamelCase (checkAccountInfo) 인데 services 코드가 PascalCase (CheckAccountInfo) 로 호출. SDK 업그레이드 시점에 서비스 코드 동기화 누락 | services/account_check_service.py:160 의 메서드명을 checkAccountInfo 로 수정 (1줄 fix). 다른 모듈(biz_check / bizinfo_check)은 이미 lowerCamelCase 라 영향 없음 | services/account_check_service.py |
| 2026-04-27 | taxinvoice/popbill-url 호출 시 Popbill -10000038 "회원의 아이디가 아닙니다" — 회원 식별 필요한 호출 전부 차단 | .env / Orbitron.yaml 에 POPBILL_USER_ID 환경변수 미설정. 메모리상 sodam2025 일반회원 ID 가 있었으나 코드에 주입 안 됨 | .env 와 Orbitron.yaml 양쪽에 POPBILL_USER_ID=sodam2025 추가 (환경변수 4곳 동기화 규칙 준수) | .env, Orbitron.yaml |
| 2026-04-27 | notifications 모듈만 popbill 환경변수 다 있어도 stub 폴백 — /urls/template-mgt·plus-friend·sender-number 가 400 "popbill 프로바이더가 활성화되어야" | services/notification_service.py 와 routers/notifications.py 가 NOTIFICATION_PROVIDER env 명시 토글 필요(다른 모듈은 LinkID/SecretKey 만 있으면 popbill 자동 활성). 이 점이 .env.example/문서에 누락 | .env 와 Orbitron.yaml 양쪽에 NOTIFICATION_PROVIDER=popbill 추가. stub 으로 토글하면 로그만 남고 미발송 동작 유지 | .env, Orbitron.yaml |
| 2026-04-29 | StatementService.registIssue() got multiple values for argument 'Memo' — 전자명세서 발행 SDK 호출 실패 | item_code 를 positional 로 넘겼는데 SDK 시그니처 `registIssue(corp, statement, Memo, UserID, EmailSubject)` 에는 ItemCode 인자가 없음. statement 객체 안 itemCode 필드여야 함 | _build_statement 에 `itemCode=draft.item_code` 추가, registIssue 호출에서 positional 제거 | services/statement_service.py |
| 2026-04-29 | 전자명세서 발행 시 -12001011 관리번호가 올바르지 않습니다 | 자동 생성된 mgt_key SDM12120260429140415c42968 (26자) 가 팝빌 24자 한도 초과 | mgt_key 형식 단축: SDM(3) + YYYYMMDDHHMMSS(14) + uuid4(4) = 21자 | routers/statement.py |
| 2026-04-29 | 팝빌 콘솔 4개 버튼 모두 "아이디 오류"로 안 열림 | 1) togo='BOX' 비표준 값 (팝빌 표준은 TBOX/SBOX/WRITE/CERT). 2) Orbitron 운영 환경에 POPBILL_USER_ID 자동 주입 안 돼 fallback 'sodam'(미등록 ID)로 호출 → 페이지 거절 | togo default 'BOX' → 'TBOX' (frontend 4 버튼 노출). fallback 'sodam' 제거하고 user_id 미설정 시 RuntimeError 명시. 사용자가 Orbitron 대시보드에 POPBILL_USER_ID=sodam2025 + NOTIFICATION_PROVIDER=popbill 직접 등록 | services/statement_service.py, routers/statement.py, pages/Statement.jsx, .env, Orbitron.yaml |
| 2026-04-29 | TaxinvoiceService.registIssue() got an unexpected keyword argument 'Memo' — 전자세금계산서 발행 SDK 호출 실패 (운영전환 미신청 -99010016 차단으로 가려져있던 잠재 버그) | SDK 1.64.1 케이스 차이: TaxinvoiceService.registIssue 시그니처는 `memo`(소문자) 인데 코드는 `Memo`(대문자) 로 호출. Statement 와 다른 케이스. 권한 풀린 후 처음 호출되며 드러남 | services/taxinvoice_service.py 의 registIssue 호출 인자를 `memo=` 로 수정 | services/taxinvoice_service.py |
| 2026-04-29 | Statement.jsx 다크 테마로 작성되어 다크 사이드바와 색조 동일 → 라벨/제목 가독성 0 (사용자 피드백 "디자인이 너무하다") | .impeccable.md 명시 "라이트 모드 전용" + 배경 #f0f4f8 가이드 위배. 자동 진행 모드에서 디자인 시스템 미확인하고 다크 토큰 사용 (bg-slate-800/50, text-slate-400 etc) — WCAG 2.1 AA 4.5:1 + 14px 최소 위배 | TaxInvoice.jsx 패턴 차용해 라이트 테마 전면 재작성: 페이지 bg-slate-50, 카드 bg-white shadow-sm border-slate-100, 입력 bg-slate-50 border-slate-200, 라벨 font-semibold text-slate-500, 양식별 conditional 영역 bg-amber-50 강조 | pages/Statement.jsx |
| 2026-04-29 | CODEF 모든 endpoint 가 SuperAdmin 계정에서 400 "사업장 정보가 없습니다" 반환 (View-As 모드인데도 거부) | 새 라우터 3개(connections/card_sync/budget) 가 admin.business_id 만 사용. 셈하나 표준 X-View-As-Business 헤더 처리 누락 (bank_sync.py 의 _resolve_bid 패턴 미적용) | routers/codef/_helpers.py 신규 — resolve_bid(admin, x_view_as_business). 11개 endpoint 모두 적용. SuperAdmin 이고 헤더 있으면 → 헤더 값, 일반 admin → admin.business_id, 둘 다 없으면 400 | routers/codef/_helpers.py, connections.py, card_sync.py, budget.py |
| 2026-04-29 | CODEF SDK 호출 시 missing-connected-id 에러 (SDK 응답은 왔는데 connectedId 추출 실패) | 1) easycodefpy 의 set_client_info() 는 PRODUCT 환경 전용 — DEMO 환경은 set_demo_client_info() 별도 호출 필요. 누락 시 SDK 자체적으로 CF-00014 클라이언트 정보 누락 응답. 2) _SUCCESS_PREFIX = "CF-000" 매칭 너무 광범위 (CF-00014 / CF-00015 / CF-00007 등 SDK 에러도 같은 prefix) → 성공으로 잘못 분류 → 비어있는 data 에서 connectedId 추출 시도 → 무의미한 missing-connected-id | set_demo_client_info() 추가 호출 (PRODUCT/DEMO 양쪽 활성). public_key 도 SDK 인스턴스에 직접 주입. _SUCCESS_PREFIX 제거 → _SUCCESS_CODES = {"CF-00000"} 정확 매칭. extraMessage 도 합쳐 에러 메시지에 포함 | services/codef/codef_client.py |
| 2026-04-29 | CF-04000 사용자 계정정보 등록 실패 (CODEF 가 페이로드 거부) — 신한카드 ID/PW 입력 시 발생 | 페이로드 clientType="B"(사업자) 하드코딩. 사용자가 입력한 ID는 개인 카드 사이트 계정(`sodam2025`) → CODEF 가 사업자 등록 형식으로 받았는데 실제 사이트는 개인 회원이라 매칭 실패. URL도 /v1/kr/card/common/b/* (사업자 경로). 사업자 가맹점 매출과 개인 카드 사용내역의 본질적 분리를 코드가 인지 못함 | _build_account_payload 에 client_type 분기 추가 (default "P" 개인). businessRegNo 는 B 모드일 때만 페이로드 포함. card_provider URL 을 /v1/kr/card/common/b/* → /p/* 로 변경 (PoC 시나리오 우선 = 개인 카드). B 모드는 후속 phase 에서 재추가 (카드 사업자 매출 영역은 이지포스 채널이 더 적합) | services/codef/connection_service.py, services/codef/card_provider.py |
| 2026-04-30 | 어드민 인사기록카드/직원관리에서 산출 시 500 오류 — Staff 모델에 없는 end_date 필드 참조 | 일부 라우터/서비스가 Staff.end_date(존재하지 않음)를 직접 참조 | 잘못된 참조 제거 + 디버그 스크립트 staff.end_date → 정상 필드 정정 | routers/hr/*, scripts/* |
| 2026-04-30 | SuperAdmin View-As 모드에서 신규 직원 추가 시 business_id=NULL 으로 생성됨 | hr/staff 라우터 신규 생성 endpoint 가 admin.business_id 만 사용. resolve_bid 헬퍼 미적용 | resolve_bid(admin, x_view_as_business) 적용 — view-as 헤더 우선 + admin.business_id fallback | routers/hr/staff.py |
| 2026-04-30 | 회사 기본정보 + 직인 관리 화면이 View-As 모드에서 빈 칸으로 뜨는 회귀 | get_business_info 등이 admin.business_id 만 사용. 4/27 view-as 패턴 누락 분 잔여 | resolve_bid 패턴 적용 | routers/auth.py |
| 2026-04-30 | sendFAX_multi 메소드명 오타 — 팝빌 SDK 호출 시 AttributeError | popbill SDK 1.64.1 은 lowerCamelCase (sendFax_multi) 인데 코드는 PascalCase 로 호출. 다른 모듈의 동일 패턴 동기화 누락 | services/fax_service.py 의 메소드명을 sendFax_multi 로 수정 (1줄 fix) | services/fax_service.py |
| 2026-04-30 | sendFax_multi 인자 순서 오류 — ReceiverName 인자 없음 에러 | 팝빌 SDK sendFax_multi 시그니처에는 ReceiverName 인자가 없음 (단건 sendFax 와 다름). receivers 리스트 안에 객체로 전달 | 인자 순서 교정 + receivers 객체 안에 receiverName 통합 | services/fax_service.py |
| 2026-04-30 | startup auto-migration 에 private_* 6 컬럼 추가 누락 — prod 배포 시 컬럼 부재로 SELECT 실패 가능성 | 새로 추가한 private_* 6 필드를 init_db.py auto-migration 에 반영 안 함 | init_db.py 의 staff 테이블 컬럼 추가 가드 6건 추가 (idempotent) | init_db.py |
| 2026-04-30 | 급여명세서 익월정산/자격미달 안내가 주휴수당 합계 0원 케이스에서 안 보이던 회귀 | 안내 분기 조건이 holiday_pay > 0 으로 되어있어 0원이면 분기 미진입 | 분기 조건을 holiday_pay >= 0 또는 별도 evaluator 사용으로 수정 | services/payroll_statement.py |
| 2026-04-30 | (사고) 4월 급여 6명 잘못 삭제 — Windows cp949 콘솔 한글 깨짐으로 직원 분류 오류 | adjust_apr_to_accountant.py 실행 시 staff 이름이 cp949 로 깨져 표시. 사용자가 깨진 이름 보고 "무관"으로 분류 → DELETE 실행 → 6명 4월 Payroll 영구 손실 위험 | 처음 조회 시 캡처한 합계값으로 즉시 복구. 이후 PYTHONIOENCODING=utf-8 prefix + staff_id 명시로 재발 방지. 메모리에 함정 영구 등록 | scripts/maintenance/restore_apr_deleted_payroll.py, memory/feedback_korean_encoding.md |
| 2026-04-30 | payroll calculate 의 override 입력 시 NameError 500 — 정의되지 않은 details 변수 사용 + details_json 에 overrides 누락 | calculate 함수 안에 details 변수 미정의 + details_json 직렬화가 별도 dict 만 사용 → 사장님이 6개 공제 필드 입력하면 산출 자체 실패 (기능 없어진 것처럼 보임) | details_payload 통합 dict 로 정정. req.overrides=None 케이스도 정상 직렬화 보장. GET endpoint 의 overrides 자동 복원도 정상 동작 | routers/payroll.py |
| 2026-04-30 | npm run dev 실행 시 백엔드(8000)만 안 뜨고 프론트 2개만 기동 | dev:backend 명령이 cmd 전용 구문(for/findstr/^|/taskkill) — Git Bash/PowerShell 에서 파싱 실패 | OS 무관 형태로 교체: `.venv\Scripts\python.exe -m uvicorn main:app --reload --host 0.0.0.0 --port 8000` (backslash + .exe 명시) | SodamApp/frontend/package.json |
| 2026-04-30 | 직원관리 카드 'ID 표시'가 staff.id (숫자) 표시 — 사장님 의도는 로그인 계정 username | 사장님 메시지의 '아이디'를 PK 로 잘못 이해 (인사기록카드 로그인 계정 sodam001 캡처로 정정 받음) | 백엔드 GET /hr/staff 응답에 username 필드 추가 (User 테이블 일괄 매핑). 프론트 카드에 username 배지 + 아이디순 정렬 | routers/hr/staff.py, pages/Staff.jsx |
| 2026-04-30 | 전자계약서 모달에서 사업주 정보(사업체명/주소/대표자/사업자등록번호) 빈 칸 + 임금 자리 빈 칸 | 1) /api/business-info 호출 → 404. 실제 경로는 /api/auth/business-info (auth.router prefix). 2) 김순복 contract_type='정규직'+monthly_salary=0 → 정규직 분기에서 0 사용 | 1) /auth/business-info 로 정정 (다른 모든 코드와 일치). 2) wage 분기 보강 — 정규직이면서 monthly_salary>0 일 때만 월급, 그 외엔 시급 fallback | utils/contractVars.js, pages/StaffDetail/index.jsx, ContractTab.jsx |
| 2026-04-30 | 전자계약서 모달 열 때 stale contractForm.content 가 본문에 잔류 (직전 변수치환 결과 빈 괄재) | handleOpenContractModal 의 template 초기값이 contractForm.content (직전 React state). fetch 부분 실패 시 stale state 가 본문으로 흘러감 | template 초기값 빈 문자열로 변경. fetch 결과만 본문 소스로 사용 — 실패 시 빈 본문이라 즉시 비정상 인지 가능 | pages/StaffDetail/index.jsx |
| 2026-04-30 | 재직증명서 본인 서명란 추가 후 발급자 블록이 2페이지로 분리 + 외곽선 하단 잘림 | 본인 서명란(약 80px) 추가로 한 페이지 콘텐츠 초과. min-height 가 가용 공간 가득 채워 외곽선이 프린터 인쇄 불가 영역(printer safe area) 에 걸림 | margin/padding 일괄 축소(@page 10/12/14mm + cert-staff-sign 14/8 + cert-issuer line-height 1.7 + 폰트 -1px). min-height calc(297-40)mm 로 축소해 안전 영역 확보 | routers/hr/certificate.py |
| 2026-05-01 | backend 가 .env 의 OPENCLAW_GATEWAY_URL=http://192.168.219.117:18790 을 무시하고 잘못된 ws://192.168.123.108:18789 로 호출 | 작업PC User scope 환경변수가 .env 보다 우선되도록 load_dotenv() 가 default override=False 로 동작 | config.py / database.py 에서 load_dotenv(override=True) — production(Orbitron) 은 .env 파일 없어 영향 없음 | config.py, database.py |
| 2026-05-01 | 사용자가 "참치김밥 매콤" 요청해도 떡볶이/소시지 같은 엉뚱한 이미지 생성. image-service 로그에 'CLIP can only handle sequences up to 77 tokens' 경고 다수 | GPT-5.5 가 50-100단어 정제 프롬프트 작성 → Flux CLIP 입력 시 끝 부분 truncation → 음식 정체성 손실 + 카메라/조명만 남아 generic Korean food → 떡볶이로 빠짐 | SYSTEM_PROMPT + CHAT_SYSTEM_PROMPT 강화: 30-50단어 MAX, 음식 정체성을 FIRST 6 words 안에 영문+로마자, 'Korean food'/'delicious'/'mouth-watering' 등 모호·필러어 금지. 검증: 36 단어 + 음식명 첫 위치 | services/openclaw_client.py |
| 2026-05-01 | 1024×1024 img2img 콜라주가 step 당 127초 → 총 6분+ 소요, frontend 240초 timeout 초과로 실패 | Flux model CPU offload 모드에서 1024 사이즈는 매 denoise step 마다 VAE encode/decode + GPU↔CPU 가중치 transfer 누적이 큰 부담 | 콜라주 target_size 1024×1024 → 512×512 (사용자가 결과 더 크게 원하면 [HD/4K 업그레이드] Real-ESRGAN 후처리), strength 0.65 → 0.55 (effective_steps 2-3 유지), axios timeout 240000 → 600000ms 안전 여유 | routers/delivery_images.py, components/AIChatPromptBuilder.jsx |
| 2026-05-12 | CODEF Phase 1 컬럼 마이그레이션 누락 → 정산 재분류 500 | init_db.py 의 cardpayment.source/cardpayment.connection_id 등 4개 + cardsalesapproval 4개 + paypayment 3개 컬럼 마이그레이션이 database.py 의 _run_codef_phase1_migrations 와 별도라 init_db 진입점에서는 누락 → production cardpayment.source 컬럼 부재 → SQLAlchemy `column does not exist` 500 | init_db._run_migrations 에 11개 컬럼 마이그레이션 추가 + production 에 수동 ALTER TABLE SQL 별도 적용 | init_db.py |
| 2026-05-12 | 정산 재분류 클릭해도 분류 안 바뀜 — 학습된 패턴이 정산 매칭보다 우선 | _classify_one_tx 우선순위가 1) 매뉴얼 2) 학습 3) 정산 매칭 4) 규칙 → 사장님이 한 번 분류한 패턴이 settlement 데이터를 가리는 burn-in | settlement 매칭을 학습보다 위로 이동: 1) 매뉴얼 2) 정산 3) 학습 4) 규칙. 추가로 카드사 약자+숫자 prefix 패턴 매칭(NH/KB/하나/롯데/SHC/신한/삼성/비씨/BC/우/현) 신규 | routers/bank_sync.py |
| 2026-05-12 | 4월 누르면 5월 데이터 표시되던 race condition | setFilter + onApply 가 async → setFilter 가 완료되기 전에 onApply 가 stale filter 로 fetch | onApply(newFilter) 가 newFilter 인자를 직접 받아 fetchTxs(customFilter) 로 전달. 같은 패턴으로 popbill 3개월 reclassify 도 함께 수정 | pages/BankSync.jsx |
| 2026-05-12 | Popbill EasyFinBank 가 3개월(90일) 이내 데이터만 제공 → 1~2월 매출 누락 | KICC EasyFinBank 정책 — 발급일로부터 90일 이내. 우아한 처리 없이 그냥 빈 응답 | skipped_months 응답 + UI 안내 ("popbill 한도 — CODEF 과거 거래 가져오기 권장"). CODEF /v1/kr/bank/.../transaction-list 로 우회 — 1687건 1~5월 데이터 일괄 import 검증 | routers/bank_sync.py, services/codef/bank_provider.py, pages/BankSync.jsx |
| 2026-05-12 | codef-pull-historical Not Found 에러 | 프론트가 마스킹된 account_number "1103577" 보냄 → 백엔드가 평문 계좌번호로 BankAccount 조회 실패 | account_id 기반 lookup 으로 변경 (DB 의 BankAccount PK 사용). 프론트 selector 도 account.id 값으로 정정 | routers/bank_sync.py, pages/BankSync.jsx |
| 2026-05-12 | CODEF 응답 remark 매핑 — resAccountDesc1 빈값, resAccountDesc3 에 거래상대방 | 초기 매핑이 desc1→remark1 직관적 가정으로 작성됐으나 실제 CODEF 응답은 desc3=거래상대방 / desc2=모바일 / desc4=점 / desc1=빈값 | 정정 매핑: desc3→remark1(메인), desc2→remark2, desc4→remark3, desc1→remark4. production 714건은 별도 SQL 로 일괄 재정렬 | routers/bank_sync.py |
| 2026-05-12 | 이지포스 로그인 endpoint 미확정 (Phase 1F) | 첫 HAR 캡처(이미 로그인된 상태)에선 checkLoginStatus 만 보이고 selectEasyPosLogin 안 발생 → 사장님 추가 HAR(로그아웃→재로그인) 캡처로 확정 | POST /cm/selectEasyPosLogin.do, body 는 dsIn Dataset(USER_ID/USER_PW/ADMIN_FG 3컬럼 N row 1줄), trailing RS 1~2개 필요 — HAR byte-perfect 일치 검증 | services/easypos_service.py, services/nexacro_ssv.py |
| 2026-05-12 | 이지포스 평문 모드(inSecureMode=0) "Login 정보가 올바르지 않습니다" | 이지포스는 USER_ID/USER_PW 를 클라이언트단에서 RSA-PKCS1v15 로 암호화해 64-byte hex 로 전송 (보안모드 inSecureMode=1) — 평문은 무조건 거부 | RSA 암호화 구현. 공개키는 checkLoginStatus.do 응답 ErrorMsg 필드의 `<modulus_hex> <exponent_hex>` 공백 구분 형식. RSA-512(64 byte modulus), exponent=10001, padding=PKCS1v15 | services/easypos_service.py |
| 2026-05-12 | RSA 구현 후에도 cookies=0 + "Login 정보 올바르지 않다" | 이지포스 백엔드 Spring/JSP — 모든 XHR 호출 전 index.jsp GET 으로 JSESSIONID 쿠키 발급이 필수. 누락 시 RSA 키쌍 매칭 실패 (서버가 응답한 modulus 와 후속 cipher 가 다른 세션) | EasyPosClient._warmup_session() 추가: index.jsp GET → JSESSIONID 자동 수집 → 인스턴스당 1회 호출 보장. _ssv_post 첫 호출에서 자동 warm-up | services/easypos_service.py |
| 2026-05-12 | 401 응답이 셈하나 axios interceptor 에서 세션 만료로 오해돼 자동 로그아웃 | routers/easypos.py 의 외부(이지포스) 인증 실패를 401 로 응답 → frontend interceptor 가 셈하나 JWT 만료로 처리 → localStorage clear + /login redirect | 외부 시스템 인증 실패는 422 로 응답. 셈하나 자체 401(JWT 만료)와 의미 구분. 빨간 박스 메시지 표시 + 자동 로그아웃 차단 | routers/easypos.py |
| 2026-05-13 | 쿠팡이츠 수동 쿠키 401 — Application 탭 직접 복사로 1개 깨진 형식 | 사장님이 F12 Application 탭의 쿠키 테이블을 텍스트로 복사 → 탭 구분 텍스트 → 우리 파서가 "name=_abck\t<value>" 1개로 잘못 인식 | 모달 안내문을 Network 탭의 cookie 헤더 사용으로 변경 + 파서가 탭/줄바꿈 best-effort 처리 (\n→;, \t→=) + 핵심 쿠키 sanity check 경고 | pages/CoupangEatsModuleDetail.jsx |
| 2026-05-13 | 쿠팡이츠 sanity check 가 EATS_AT/RT 만 인식 → unify-token 누락 경고 | 2026년 쿠팡이츠 신버전 인증이 EATS_AT/RT 통합 → unify-token + account-id. HAR 분석 시점엔 모호했음 | sanity check 에 unify-token / account-id / bm_* 패턴 추가. FE + BE manual-cookies + debug/probe 모두 갱신 | pages/CoupangEatsModuleDetail.jsx, routers/coupang_eats.py |
| 2026-05-13 | fetch_orders 500 — AttributeError: 'NoneType' object has no attribute 'get' | raw["data"] 가 None 일 때 우리 unwrap 로직 처리 없음. HAR 응답({"totalSalePrice":...}) 과 실제 응답 구조 다름 | fetch_orders/fetch_settlements 둘 다 None / 비-dict / 빈 응답 안전 처리. JSON 파싱 실패 시 body 200자 포함 에러 메시지. /debug/raw-orders endpoint 추가로 응답 raw 확인 가능 | services/coupang_eats_service.py, routers/coupang_eats.py |
| 2026-05-13 | 쿠팡이츠 주문 0건 — 실제 14건 (응답 OK인데 우리 파싱이 빈 결과) | upsert_orders 가 잘못된 필드명 찾음. HAR 응답엔 orderedAt/totalSalePrice 가정했지만 실제는 createdAt(epoch ms)/totalAmount/status. partialCanceled/canceledAmountMoney 도 누락 | upsert_orders 필드 매핑 정정: createdAt(NOT orderedAt), totalAmount/totalAmountMoney.units (NOT totalSalePrice), status (NOT orderStatus). cancelled 판정에 partialCanceled + canceledAmount>0 포함 | services/coupang_eats_service.py |
| 2026-05-13 | 쿠팡이츠 주문 여전히 0건 — fetch_all_orders 가 100건 page_size 로 호출 시 빈 응답 | 쿠팡이츠 API 가 pageSize=100 거부 (HAR 페이로드는 항상 pageSize=10). DB 직접 조회로 CoupangEatsOrder=0, CoupangEatsSettlement=54 확인하면서 발견 | fetch_all_orders default page_size 100 → 10, max_pages 50 → 500 (10*500=5000건 한도). orderPageVo 추출 시 raw / raw.data 양쪽 시도 (wrapping 대응). 페이지마다 INFO 로깅 | services/coupang_eats_service.py |
| 2026-05-13 | cron 즉시 테스트 결과 target_date=2026-05-11 (어제는 5/12 인데 1일 빠름) | 호스트 timezone KST 인데 컨테이너 timezone UTC. KST 오전이 UTC 전일 23시대 → date.today() 가 KST 보다 1일 빠름 → today-1 도 1일 어긋남. 실제 cron 새벽 03:00 KST 실행시에도 동일 1일 차이 발생 | Dockerfile 에 tzdata 추가 + ENV TZ=Asia/Seoul + /etc/localtime 링크. 컨테이너의 모든 date 계산이 KST 기준으로 통일. 재배포 후 cron 자동 정확 | Dockerfile |
